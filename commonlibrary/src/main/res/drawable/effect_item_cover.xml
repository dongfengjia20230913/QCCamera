<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle"
    android:useLevel="false">
    <size
        android:width="@dimen/effect_list_item_mask_image_size"
        android:height="@dimen/effect_list_item_mask_image_size" />
    <!--<corners android:radius="4dp" />-->
    <stroke
        android:width="1dp"
        android:color="@color/main_color" />

    <corners
        android:radius="4dp"
        />
</shape>